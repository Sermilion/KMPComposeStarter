## [2026-08-29] KMP-1 subtask 5 — navigation correctness (back stack ownership, restoration, tab retention)
Areas: composeApp/navigation, composeApp/ui, androidApp (manifest), feature/{home,auth,profile}, gradle (catalog), docs/architecture
- Net 39 files, +1164/-206. The navigator is the only writer of navigation state: `NavDisplay`'s default `onBack` pops the `SnapshotStateList` in place, mutating the rendered stack while the navigator's `StarterNavigationState` goes stale. Rule — if a display library ships a default back handler owning the stack, pass your own `onBack` or accept two sources of truth.
- Tab retention forced abandoning the single-display/list-swap design: entries absent from the list handed to `NavDisplay` are popped, so swapping the list on a tab switch destroyed every ViewModelStore and SaveableStateHolder. Fix is one `rememberDecoratedNavEntries` per tab, called unconditionally over `TopLevelTab.entries` — the call count must never become conditional.
- One decorator list is shared across all four stacks (the plan sketched per-tab lists). Verified against navigation3-runtime source: `onPop` fires only when a contentKey leaves that call's own backstack, and keys cannot collide because `navigate()` rejects tab roots. **That rejection is load-bearing for state retention, not hygiene — do not relax it.**
- `NavEntrySavedStateDecorator` is hand-rolled, marked `shortcut:`: no released navigation3 artifact ships `SavedStateNavEntryDecorator`, `ViewModelStoreNavEntryDecorator` demands an owner still `INITIALIZED`, and `LocalSavedStateRegistryOwner` has **no value at all** on desktop/iOS (Android's is merely past `INITIALIZED`). Decorator order is load-bearing: saveable-state → SavedState owner → ViewModel store.
- Saver asymmetry worth copying: encode strictly through `starterSerializersModule` so a missing route throws in development, restore leniently — `SerializationException` returns null, `ignoreUnknownKeys` on, since an undecodable payload from a `Saver` is an app-update crash loop the user cannot clear. Empty stacks restore to the flow's start route; tabs keyed by enum `name` so a fork removing a tab drops one key, not the whole restore.
- Back policy is split by design and documented as such: `NavDisplay` owns in-stack pops, disabling its handler when the stack empties; the host `NavigationBackHandler` owns only the "non-HOME tab root → HOME" rule and stays enabled through the session hand-off so a stray press cannot close the app. HOME root falls through to the system.
- Tab-switch transitions classify on previous-tab ≠ current-tab (destination shape mislabelled every switch into a deeper stack). Previous tab lives in a plain class outside the snapshot system on purpose — a snapshot write from `SideEffect` re-invalidates the composition and flips the spec mid-animation.
- `DetailRoute(id)` is the first consumer of subtask 2's assisted-args machinery: typed arg keyed by constructor parameter name plus `@Assisted savedStateHandle` — the template's answer to "how does a nav argument reach a ViewModel". The tautological `isLoggedIn` parameter is gone from `StarterApp`/`StarterNavDisplay`/the entry provider; gating reads session state.
- Three new catalog aliases, pre-release pinned: `navigationevent-compose` 1.0.0-rc01 (the back API `NavDisplay` itself dispatches through — chosen over `androidx.compose.ui.backhandler` for that reason) plus `androidx-savedstate`/`-compose` 1.4.0. Coordinates came from cached module metadata, not a resolution run.
### [2026-08-29] KMP-1 subtask 5 — limitations, flags, acceptance
- Known limitations / validate watch-list: tab-state retention is **argued, not asserted** — no UI test, and navigation.md says so; do not report it as verified. Unproven by inspection: whether the desktop/iOS `SaveableStateRegistry` accepts the `SavedState` the entry-owner `Saver` writes, and whether the new aliases resolve. Manual smoke (Android rotation, predictive back at both root kinds, desktop back) was NOT EXECUTED.
Feature flag: N/A
Acceptance criteria: 6/7 implemented (criterion 7 — `./gradlew check` — is the validate gate; criterion 3's retention half rests on the documented navigator-level equivalent the criterion permits, not a UI test)

## [2026-08-29] KMP-1 subtask 4 — build logic, tooling, and CI honesty
Areas: build-logic/convention, config/detekt, gradle (catalog+properties), root build, settings, .editorconfig, .github (workflows+dependabot), androidApp, composeApp, codegen/viewmodel-inject-processor, core/*, feature/*, docs/architecture
- Net 35 files, +519/-665 — this pass mostly deletes. Largest reusable finding: `config/detekt/detekt.yml` (582 lines) was never loaded — `config.setFrom` was guarded on a per-module `detekt.yml` that no module has, and `check` depended on `detektMetadataCommonMain` alone. A quality config can stay dead for a repo's whole life without one failing build — assert the load unconditionally and wire `check` to `tasks.withType<Detekt>()`, never a single named task.
- Detekt path excludes compare `it.file.invariantSeparatorsPath`, not `absolutePath`: a literal `"/build/"` check silently never matches on Windows. Same bug class as spotless's `targetExclude("${layout.buildDirectory}/**/*.kt")`, which interpolated a `Directory` provider into a path matching nothing.
- Spotless gotcha: two `targetExclude` calls **replace** each other rather than accumulating — that is how the build-directory exclusion went missing. `editorConfigOverride` was deleted outright rather than trimmed; ktlint reads `.editorconfig` directly, so one file is the whole style contract and build-logic's spotless reuses it with no duplicated map.
- Android targets are configured once in build-logic and the namespace is **derived from the Gradle project path**, so no `kotlin { android { } }` blocks remain outside build-logic — not "one namespace line per module" as planned. androidApp keeps `com.android.application` only for applicationId/buildTypes/packaging. Room's KSP compiler routes through `kmp.kotlininject` gated on `pluginManager.withPlugin("androidx.room3")`, so no module declares a `ksp*` dependency.
- Included builds are invisible to the root gate: root `check` now `dependsOn(gradle.includedBuild("build-logic").task(":convention:check"))`, and Kover aggregates via `dependencies { subprojects.forEach { kover(it) } }` with build-logic deliberately outside it (separate build, not a subproject). Merged report lands at `build/reports/kover/`.
- Compose-resource-before-KSP ordering in core:ui stays name-matched on purpose: the Compose generator task types are `internal` in the Compose Gradle plugin, so a typed `withType` reference is impossible. The reason sits at the call site so a future reader does not "fix" it into a compile error.
- Kept with reason: `composeMaterial3` stays on `1.10.0-alpha05` because that line publishes no stable release (availability-driven, not API-driven — no expressive API is used); desktop packaging keeps `java.sql` + `jdk.unsupported` after dropping `includeAllModules`, since `packageDistributionForCurrentOS` is not exercised by `check` and a wrong trim is an unverifiable runtime failure.
- Breaking for forks: `org.gradle.configuration-cache=true` is on and `configuration-cache.problems` no longer relaxed, so new build logic must be CC-safe or the build fails rather than warns. Every catalog alias is now referenced — a fork adding a dependency must add the alias and the usage together.
### [2026-08-29] KMP-1 subtask 4 — validate watch-list, flags, acceptance
- Validate watch-list (none provable by inspection): detekt's shared config runs for the first time at `build.maxIssues 0` across every module — triage findings by fixing them or scoping the rule narrowly with a comment, never a baseline or blanket suppression. Also unproven: Kover 0.9.3 under AGP 9.1, the included-build `check` dependency, and whether deleting composeApp's `name.contains("Jvm")` dependsOn/srcDir hacks and core:data's KMP-android `RestrictedApi` disable breaks compilation or re-surfaces the lint. Compare the executed Test-task count against a pre-change run so a lost `useJUnitPlatform()` shows up as missing tests, not a green build.
Feature flag: N/A
Acceptance criteria: 6/7 implemented (criteria 1-6 structurally complete; criterion 7 — `./gradlew check` plus the iOS simulator framework link — is the validate gate, as is criterion 5's "passes twice in a row" store+reuse proof. NOT EXECUTED here)

## [2026-08-29] KMP-1 subtask 3 (completion pass) — terminal delete-my-data contract
Areas: core/data (db, repository), feature/profile, docs/architecture
- The whole behavioral change is one operator: `ProfileViewModel`'s `observeCurrentUser()` collector gained a `.catch` that logs and stops following the stored row. Without it a delete that closed the database and then failed to unlink the files left the collector throwing into `viewModelScope` and taking the session down. Net 5 files, +43/-6; the other four are contract text.
- Reusable rule this pass settled: a delete-my-data path that must close the DB before unlinking its files is **terminal whichever way it returns**. Room hands out one instance and a closed one cannot be un-closed, so a `false` is not retryable into a working handle. Model such a call as "session stays signed in, reports the failure, and can no longer read its own row" — never as recoverable, and never as a reason to sign the user out.
- Considered and rejected: making the handle recoverable. That needs re-plumbing `StarterUserDao`'s database resolution and re-establishing the Room flow after a failed delete — larger than the gap it closes — and close-before-delete is mandatory on all three platforms (unlinking a file an open connection holds either fails outright or leaves that connection writing to an orphaned inode).
- The contract is now stated at every site that carries it: `DatabaseProvider.deleteDatabaseForUser` KDoc, the close call in `StarterRoomDatabaseProvider`, `StarterUserRepository.deleteMyData` KDoc, `ProfileViewModel.deleteMyData` KDoc, and `docs/architecture/persistence.md`. Reusable: a contract spread across a provider → repository → ViewModel chain goes stale unless each link restates it; one KDoc at the interface is not enough.
- Deliberately no new test: a deterministic file-deletion failure is not reproducible on the JVM host without mocking the platform builder factory, and the `.catch` does not change `ProfileViewModelTest`'s observable contract (stays signed in, reports failure, never logs out), so the existing test needed no update either.
- Known limitation and review watch: the `.catch` is untyped, so any upstream repository error — not only a post-delete read failure — degrades to a stale profile plus one Kermit warning instead of a visible error. Acceptable for a starter with one failure mode on this flow; a fork that adds more should narrow it.
- Validate risk carried forward from subtask 2: the KSP `dependsOn(generateNetworkConfig)` ordering is still inspection-reasoned, not compile-proven — an unresolved `NetworkConfig` on any target has that as its named cause, not a flake. ~85 branch files have not passed spotless/detekt, so formatting churn on the first gate run is expected rather than a defect signal.
Feature flag: N/A
Acceptance criteria: 6/7 implemented (criterion 7 — `./gradlew check` — is the validate phase's gate; criterion 1's manual desktop smoke was NOT executed, `SessionRestorerTest` covers the restore-decision path as the automated substitute)

## [2026-08-29] KMP-1 subtask 2 (completion pass) — KSP generator ordering + networking doc honesty
Areas: core/data, docs/architecture
- Verification pass, not a rewrite: the subtask-2 payload was already committed (`1da5330`), so criteria 1-6 were re-confirmed by source inspection and left in place. Net diff is 2 files, +12/-0, both additive.
- Reusable Gradle gotcha: `kotlin.sourceSets.commonMain { kotlin.srcDir(generatorTask) }` reliably orders the *Kotlin compile* tasks behind the generator, but the KSP tasks read the same source roots through a file collection that does not always carry the builder dependency — KSP can run before the generated `NetworkConfig.kt` exists and fail on an unresolved reference. Fix is an explicit `tasks.matching { it.name.startsWith("ksp") }.configureEach { dependsOn(generateNetworkConfig) }`; use lazy `matching`, not eager `withType`, and expect this in any module that pairs generated sources with KSP.
- Docs honesty (reusable): `conventions.md` now states that no shipped code path issues a real request while `MockAuthRemoteDataSource` is the only binding — the client is built, configured and tested, but nothing injects it until a fork swaps the binding. The wording was narrowed mid-pass after reading `StarterHttpClientTest`: it pins bearer-token attachment and base-URL resolution but NOT the `Logging` sanitization, so do not claim test coverage a doc bullet has not verified.
- Verified and deliberately not regenerated: the committed Room export at `core/data/schemas/.../UserDatabase/1.json` matches `UserEntity` column-for-column at version 1 with the `MIGRATION_1_2` template present.
- Deliberate no-op: kotlinx-datetime is pinned at 0.6.2 where `Clock.System.now()` is not deprecated, and `warningsAsErrors` is undefined in `gradle.properties` so `allWarningsAsErrors` resolves to false — the two call sites were left alone rather than churned for a warning that is not an error here.
- Validate risk carried forward: the KSP ordering fix is inspection-reasoned, not compile-proven. Stale KSP output under `core/data/build` from the pre-rename tree still names deleted symbols (`MockAuthApiService`, `OnboardingDatabase`, `KtorAuthRemoteDataSource`, `StarterAuthLocalDataSource`) — re-check any failure against a clean build before treating it as a real defect. Roughly 85 branch files have not yet passed spotless/detekt, so formatting churn on the first gate run is expected.
- Known limitation: the manual session smoke (sign in, kill the process, relaunch, land on Home with no login flash) is a human step and was NOT executed; `SessionRestorerTest` covers the restore-decision path as the automated substitute.
Feature flag: N/A
Acceptance criteria: 6/7 implemented (criterion 7 — `./gradlew check` — is the validate phase's gate, not an implementation item)

## [2026-08-29] KMP-1 subtask 1 (completion pass) — residual dead code + pre-auth composition-local rename
Areas: core/data (common+jvm), core/common/di, core/ui/navigation, composeApp, config/detekt, docs/architecture
- Residual removals, no behavior change: deleted seven zero-consumer core:data files left over from the first pass — `AuthApiService`, `MockAuthApiService`, `InMemoryTokenStore`, `UserDataModel`, `StarterAuthLocalDataSource`, `KtorAuthRemoteDataSource`, and the jvm `JdbcSqliteDriver`. Net -387/+4 lines across 10 files.
- Pre-auth ViewModel factory unified under one name, `LocalPreAuthViewModelFactory`: declared in core:common `CompositionLocals.kt`, read by core:ui `injectPreAuthScopedViewModel`, provided by composeApp `StarterRoot`. Zero occurrences of either old name remain.
- Correction to the 2026-08-27 entry below: it recorded "deleted `LocalViewModelFactory`, kept `LocalViewModelProvider`". Neither name exists now; `LocalPreAuthViewModelFactory` is the authoritative end state.
- Reusable: renaming a composition local here is a four-surface change, not a symbol refactor. `config/detekt/detekt.yml` `CompositionLocalAllowlist` and `docs/architecture/di-and-scoping.md` both name locals as literal strings, so a symbol-only rename leaves a detekt failure plus a stale doc; import blocks also need re-sorting because ktlint/spotless enforces lexicographic order and the new name sorts before `LocalScreenComponentFactory`.
- Reusable: make the rename substitution idempotent (normalize any already-renamed token before renaming) so a retried run cannot produce a doubled identifier, then sweep tracked *and* untracked files — `git grep` alone misses the untracked half of an in-progress payload.
- Commit-scope limitation: this change shares a working tree with the in-progress subtask-3 payload (core:datastore, `DataStoreAuthLocalDataSource`, `HttpClientModule`, per-user `UserDatabase`). The doc edits that mark those subsystems as shipping are honest only if that code lands in the same commit; a subtask-1-only commit would require restoring them to "wired in KMP-1" markings first.
- Validate risk: stale KSP output under `core/common/build/kspCaches` still names symbols deleted in earlier subtasks, and the rename crosses core:common, core:ui and composeApp on every KMP target — a clean or invalidated build may be needed before `check` is meaningful.
Feature flag: N/A
Acceptance criteria: 3/4 implemented (criterion 4 — `./gradlew check` plus the manual desktop smoke — is the validate phase's gate; NOT EXECUTED here)

## [2026-08-28] KMP-1 subtask 2 — boundaries, DI lifecycle, ViewModel machinery, codegen hardening
Areas: core/domain, core/data, core/common, core/ui, composeApp (+ios/jvm/android hosts), androidApp, feature/{auth,home,profile,settings}, codegen/viewmodel-inject-processor, build-logic/convention, gradle, docs/architecture
- Dependency rule restored: `AuthRepository`, `LoginResult`, `TokenStore`, `DemoCredentials` and the sealed `AuthError` now live in core:domain; `StarterAuthRepository` binds from data via `@ContributesBinding`. No feature module depends on core:data, and core:data exposes no `api()`. `UserData` lost its token and `@Serializable`.
- One domain error type replaces the three parallel error shapes; ViewModels map typed reasons to UI strings and no user-facing English remains in data. `withRestErrorHandling` rethrows `CancellationException`.
- Session lifecycle (reusable pattern): `StarterUserComponentManager` is a single StateFlow source of truth whose transitions run under an atomicfu lock, so a sign-in can never overtake a concurrent sign-out and teardown is exactly-once. Teardown is a multibound `UserScopedCloseable` set plus a `@SingleIn(UserScope)` `CoroutineScope`; `logout()` tears down in a `finally` regardless of the network call's outcome.
- ScreenScope is real: `ScreenComponentHolder` lives in the nav entry's `ViewModelStore`, so `@SingleIn(ScreenScope)` shares within a screen and dies with the entry. `rememberNavEntryViewModelStoreOwner` (core:ui, reusable) survives configuration changes but drops all entry stores at session end — Navigation 3 keys stores by content key, so without it the next user would inherit the previous session's screen components.
- One ViewModel factory, one registration mechanism: `DIViewModelFactory` and the old `ViewModelFactory` are gone, `StarterViewModelFactory` is parameterized by scope with `associateBy` lookup and loud duplicate detection; hand-written `AppScopedViewModelModule` deleted — the processor now registers every ViewModel through the dedicated `@ContributesViewModel(scope)` annotation instead of overloading `@SingleIn`.
- Assisted-args key contract fixed on both sides: typed args are keyed by constructor parameter name, and `@Assisted savedStateHandle` is required rather than resolved by name-magic with a swallowed exception.
- Processor hardening: the DI package is a KSP option (`di.package`) wired through `KmpKotlinInjectConventionPlugin`; unknown scopes fail the build via `logger.error`; invalid symbols are deferred across rounds; `Dependencies(aggregating = false)` with a null-safe `containingFile`; `printStackTrace` dropped. New compile-testing suite covers happy path, assisted params, SavedStateHandle, unknown-scope failure, and deferral.
- Platform hosts deduped: shared commonMain `AppComponent` + one `StarterRoot(component)` composable replace three copy-pasted wiring blocks; iOS/JVM graphs moved out of `remember { }` into process-level holders matching Android's Application-owned lazy; the two byte-identical iOS factory actuals collapsed into `iosTargetMain`. App scope gained a Kermit-logging `CoroutineExceptionHandler` behind an `UnhandledErrorReporter` seam.
- Known limitation: `TokenStore` is an in-memory binding and demo credentials are still injected constants — subtask 3 backs both with DataStore/real networking. Assisted-args and SavedStateHandle have codegen coverage but no consuming exemplar until subtask 5's `DetailRoute`.
Feature flag: N/A
Acceptance criteria: 6/7 implemented (criterion 7 — `./gradlew check` — is the validate phase's gate, not an implementation item)

## [2026-08-27] KMP-1 subtask 1 — dead-code deletion and docs honesty
Areas: core/common, core/data, core/designsystem, core/ui, core/datastore, core/domain, composeApp/navigation, feature/{home,profile,settings}, build-logic/convention, codegen/viewmodel-inject-processor, config/detekt, gradle, docs
- Removals only, no behavior change: every deleted symbol was grep-verified to have zero consumers before deletion. Net -855/+82 lines across 55 files.
- Deleted dead navigation/DI surface: `RouteRegistration`, `LocalViewModelFactory` (kept `LocalViewModelProvider`), the custom `AppScope` annotation (real code uses the anvil one; `UserScope`/`ScreenScope` kept), the `UserScopedRoute` marker and its implements clauses, `StarterTransitions` (StarterNavDisplay's inline specs are now the single transition vocabulary).
- Deleted the entire `OnboardingDatabase` stack (entity, DAO, expect/actual builders, provider cache branch, test, committed schema export) — no onboarding feature exists. Renamed `ViewModelScope.Onboarding` to `PreAuth`; no `Onboarding`-named symbol remains.
- Deleted the unconsumed theming surface in core:designsystem (`MediaTheme`, `KmpContentAlpha`, `KmpColors`/`KmpTheme` object/`KmpSystemBarsTheme`, the text-field color helpers and ~240 lines of unused constants). `StarterTheme`, scheme, `Shape`, `Typography` intentionally kept — subtask 6 rebuilds on top of them.
- Deleted dead build-logic: `PrintTestApks`, `KmpJacocoConventionPlugin` + `Jacoco.kt` and its six per-module applications — verified a silent no-op (zero jacoco tasks existed). Coverage is intentionally absent until Kover lands in subtask 4.
- Deleted dead contract members: `HomeContract.UiState.isLoading`, `SettingsContract.Event.NavigateBack` + `SettingsViewModel.navigateBack` + its unreachable collection branch in `StarterEntryProvider`.
- Docs honesty pattern (reusable): docs now describe only what exists; still-unwired subsystems (DataStore, per-user DB, serializers) are explicitly marked "wired in KMP-1" rather than silently deleted, so later subtasks have an anchor. Created the missing `docs/window-insets.md` that CLAUDE.md referenced five times; corrected stale AGP/Gradle version claims.
- Known limitation: the starter has no coverage tooling and a thinner theme API until subtasks 4 and 6 land. Non-goals honored — auth data sources, serializers module, `DatabaseProvider`, `StarterUserDao`, `UserDatabase`, `core:datastore`, `UserMapper` all retained for subtasks 2/3/5.
Feature flag: N/A
Acceptance criteria: 3/4 implemented (criterion 4 — `./gradlew check` plus desktop smoke — is the validate phase's gate, not an implementation item)
